{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": [
    "server_url",
    "api_key",
    "instance_id"
  ],
  "properties": {
    "server_url": {
      "type": "string",
      "title": "Server URL",
      "description": "Base URL for the IBM Concert instance",
      "minLength": 1
    },
    "api_key": {
      "type": "string",
      "title": "API Key",
      "description": "API key used to authenticate to IBM Concert",
      "x-sensitive": true,
      "minLength": 1
    },
    "instance_id": {
      "type": "string",
      "title": "Instance ID",
      "description": "IBM Concert instance identifier sent with API requests",
      "minLength": 1
    },
    "verify_ssl": {
      "type": "string",
      "title": "Verify SSL",
      "description": "Verify SSL certificates when connecting to IBM Concert",
      "enum": [
        "true",
        "false"
      ],
      "default": "true"
    }
  }
}